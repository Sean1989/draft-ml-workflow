import pandas
from sklearn.svm import LinearSVC

data = pandas.read_csv('../data/accurate-dataset.csv')
n_features = 2
n_samples = len(data)

clf = LinearSVC()
X = data.ix[:, :'y']
y =  data.ix[:, 'result':]

